services:
  portfolio:
    build: .
    container_name: fotograf-portfolio
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - ./public/uploads:/app/public/uploads
